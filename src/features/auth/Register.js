import { Link } from "react-router-dom";

const Register = () => {
  const content = (
    <section className="h-screen">
      <h1>Register Page</h1>
    </section>
  );

  return content;
};

export default Register;
