import { Link } from "react-router-dom";

const Login = () => {
  const content = (
    <section className="h-screen">
      <h1>Login Page</h1>
    </section>
  );

  return content;
};

export default Login;
